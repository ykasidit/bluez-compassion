* if more than 1 connected then warn about competing for stdin

* specify pipe naming for multiple connections like:
   param: /dev/rfcomm shall be

* re-implement set device class through python-dbus?

